#include "main.h"

/**
 * rev_string - reverses order of string
 * @s: string to print
 */
void rev_string(char *s)
{
	int a, b;

	a = 0;

	while (s[a] != '\0')
		a++;

	for (b = a - 1; b >= 0; b--)

	s = b;
}
